version: "3.8"

services:
  mysql:
    build:
      context: ../mockDB
      dockerfile: Dockerfile
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 10s
      retries: 5
